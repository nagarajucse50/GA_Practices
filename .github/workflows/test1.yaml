name: testing jobs

on: push

jobs:
  files:
    runs-on: ubuntu-latest
    steps:
      - name: create a one file
        run: echo "Nagaraju welcome to devops" > devops.txt

      - name: display the content of the file
        run: cat devops.txt

      - name: display the date
        run: date

      - name: display the present location
        run: pwd
        

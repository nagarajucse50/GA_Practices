name: Testing Action

on: push

jobs:
    test-job:
        runs-on: ubuntu-latest
        
        steps:
            - name: Hello World
              run: echo "hello world"

            - name: display the date
              run: date